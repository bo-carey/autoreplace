{
	"private": true,
	"scripts": {
		"build": "parcel build source/manifest.json --no-content-hash --no-source-maps --no-cache --detailed-report 0",
		"lint": "run-p lint:*",
		"lint-fix": "run-p 'lint:* -- --fix'",
		"test": "run-p lint:* build",
		"watch": "parcel watch source/manifest.json --no-cache --no-hmr",
		"check": "tsc --noEmit"
	},
	"browserslist": [
		"last 1 Chrome version",
		"last 1 Firefox version"
	],
	"dependencies": {
		"webext-options-sync": "^2.0.1",
		"webextension-polyfill": "^0.8.0"
	},
	"devDependencies": {
		"@parcel/config-webextension": "^2.0.0-rc.0",
		"@parcel/transformer-image": "^2.0.0-rc.0",
		"@types/chrome": "0.0.158",
		"@types/node": "^16.9.6",
		"@types/webextension-polyfill": "^0.8.0",
		"npm-run-all": "^4.1.5",
		"parcel": "^2.0.0-rc.0",
		"typescript": "^4.4.3",
		"web-ext-types": "^3.2.1"
	}
}
